.DS_Store
.claude/
__pycache__/
.pytest_cache/
.mypy_cache/

.venv/
.venv311/

frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
backend/build/
backend/*.egg-info/

# Rendered media artifacts (ffmpeg CompositionService default output dir).
renders/
backend/renders/

# Per-run structured artifact + log sink output (FileRunArtifactSink, ADR 0057).
backend/runs/

.env
.env.*
!.env.example

# Local-only agent brief; not tracked in VCS.
REEL_AGENT_INSTRUCTIONS.md

# Local secrets — never commit.
keys.md
*.key
secrets.*
# Certificate / key material (defense-in-depth; CodeRabbit suggestion).
*.pem
*.crt
*.cer
*.p12
*.pfx
__keys__.md

# Local TTS model artifacts (downloaded per .env.example one-time setup; ~340MB).
backend/kokoro-v1.0.onnx
backend/voices-v1.0.bin
